{
  "name": "@kshema/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared interfaces, DTOs, and Zod schemas for the Kshema Ambient Safety Platform",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"@kshema/types lint placeholder\"",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist .turbo *.tsbuildinfo || true"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}

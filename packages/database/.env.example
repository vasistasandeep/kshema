# Local Prisma connection for @kshema/database.
# Copy to `.env` (gitignored) for `prisma migrate` / `prisma db seed`.
# Matches the docker-compose Postgres 16 service in the repo root.
DATABASE_URL=postgresql://kshema:kshema_dev_password@localhost:5432/kshema?schema=public

{
  "name": "@kshema/encryption",
  "version": "0.1.0",
  "private": true,
  "description": "AES-256-GCM + RSA-OAEP zero-knowledge envelope crypto (Flight Recorder + emergency dossier)",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./decrypt": "./src/decrypt.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"@kshema/encryption lint placeholder\"",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist .turbo *.tsbuildinfo || true"
  },
  "dependencies": {
    "hash-wasm": "4.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "fast-check": "3.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}

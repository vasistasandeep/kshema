{
  "name": "@kshema/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Brand design tokens + NativeWind/Tailwind primitives (scaffold; completed in task 1.5)",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"@kshema/ui lint placeholder\"",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist .turbo *.tsbuildinfo || true"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
